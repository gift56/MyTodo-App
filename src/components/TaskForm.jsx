import React from "react";

const TaskForm = () => {
  return (
    <form>
      <button>+</button>
      <input type="text" placeholder="Your next task..." />
    </form>
  );
};

export default TaskForm;
